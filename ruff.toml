line-length = 120
target-version = "py311"

[lint]
select = ["E", "F", "I", "N", "W", "UP"]
ignore = ["UP035", "UP006"]

[lint.per-file-ignores]
"src/coddy/webhook/server.py" = ["N802"]

[lint.isort]
known-first-party = ["coddy"]
